import React from "react";
import "@/app/pages/Contacts/Contacts.scss";

type Props = {};

const Contacts = (props: Props) => {
  return (
    <div className="Contacts">
      <h1>This is a contacts page</h1>
    </div>
  );
};

export default Contacts;
